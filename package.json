{
  "name": "cachicamoapp-for-woo",
  "version": "1.0.0",
  "description": "Connect Cachicamo with WooCommerce for inventory sync and order management",
  "scripts": {
    "watch": "nodemon --watch . --ext php --ignore vendor/ --exec \"composer run fix-cs\"",
    "build": "npx wp-scripts build"
  },
  "devDependencies": {
    "@wordpress/scripts": "^30.14.0",
    "@wordpress/components": "^25.12.0",
    "@wordpress/i18n": "^4.45.0",
    "chokidar": "^3.6.0",
    "nodemon": "^3.1.0"
  }
}
